<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Child Limit Reached</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root {
      --gold: #D4AF37;
      --bg: #0b0b0b;
      --panel: #141414;
      --border: #222;
      --muted: #9aa3ad;
      --radius: 14px;
    }
    body {
      margin: 0;
      font-family: system-ui, -apple-system, "Segoe UI", Arial;
      background: var(--bg);
      color: #fff;
    }
    main {
      max-width: 520px;
      margin: 80px auto;
      padding: 0 20px;
      text-align: center;
    }
    .panel {
      background: var(--panel);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 32px 24px;
    }
    .icon { font-size: 48px; margin-bottom: 12px; }
    h1 { font-size: 24px; margin-bottom: 8px; }
    .msg {
      color: var(--muted);
      font-size: 15px;
      line-height: 1.6;
      margin-bottom: 24px;
    }
    .btn {
      display: inline-block;
      background: var(--gold);
      color: #000;
      padding: 12px 24px;
      border-radius: 10px;
      font-weight: 800;
      text-decoration: none;
      font-size: 15px;
      margin: 6px;
    }
    .btn:hover { opacity: 0.9; }
    .btn-outline {
      background: transparent;
      border: 2px solid var(--gold);
      color: var(--gold);
    }
  </style>
</head>
<body>
  <main>
    <div class="panel">
      <div class="icon">👨‍👩‍👧‍👦</div>
      <h1>Child Limit Reached</h1>
      <p class="msg">
        Your <strong>{{currentPlan}}</strong> plan allows a maximum of
        <strong>{{maxChildren}} children</strong>.
        {{#if (eq currentPlan "Silver")}}
          Upgrade to <strong>Gold</strong> to add up to 5 children.
        {{/if}}
        {{#if (eq currentPlan "Free Trial")}}
          Subscribe to a plan to start adding children.
        {{/if}}
      </p>
      <a href="/parent/dashboard" class="btn">← Back to Dashboard</a>
      {{#if (eq currentPlan "Silver")}}
        <br><br>
        <form method="POST" action="/payments/paynow/init" style="display:inline">
          <input type="hidden" name="plan" value="gold">
          <button class="btn" type="submit">⬆️ Upgrade to Gold - $10/mo</button>
        </form>
      {{/if}}
    </div>
  </main>
</body>
</html>
