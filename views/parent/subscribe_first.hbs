<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Subscribe to Continue</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root {
      --gold: #D4AF37;
      --silver: #C0C0C0;
      --bg: #0b0b0b;
      --panel: #141414;
      --border: #222;
      --muted: #9aa3ad;.
      --radius: 14px;
      --green: #1db954;
    }
    body {
      margin: 0;
      font-family: system-ui, -apple-system, "Segoe UI", Arial;
      background: var(--bg);
      color: #fff;
    }
    main {
      max-width: 620px;
      margin: 60px auto;
      padding: 0 20px;
      text-align: center;
    }
    .icon { font-size: 56px; margin-bottom: 12px; }
    h1 { font-size: 26px; margin-bottom: 8px; }
    .msg {
      color: var(--muted);
      font-size: 15px;
      line-height: 1.6;
      margin-bottom: 28px;
      max-width: 480px;
      margin-left: auto;
      margin-right: auto;
    }

    .plan-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 16px;
      margin-bottom: 24px;
    }

    .plan-card {
      background: #111;
      border: 2px solid var(--border);
      border-radius: 14px;
      padding: 24px 18px;
      text-align: center;
    }
    .plan-card.silver-border { border-color: var(--silver); }
    .plan-card.gold-border { border-color: var(--gold); }

    .plan-badge {
      display: inline-block;
      padding: 4px 14px;
      border-radius: 20px;
      font-size: 12px;
      font-weight: 800;
      text-transform: uppercase;
      margin-bottom: 8px;
    }
    .badge-silver { background: var(--silver); color: #000; }
    .badge-gold { background: var(--gold); color: #000; }

    .plan-price { font-size: 32px; font-weight: 900; margin: 4px 0; }
    .plan-period { color: var(--muted); font-size: 13px; margin-bottom: 8px; }
    .plan-detail { color: #ccc; font-size: 14px; margin-bottom: 14px; }

    .btn {
      display: inline-block;
      background: var(--gold);
      color: #000;
      padding: 12px 24px;
      border-radius: 10px;
      font-weight: 800;
      text-decoration: none;
      font-size: 15px;
      border: none;
      cursor: pointer;
    }
    .btn:hover { opacity: 0.9; }
    .btn-silver { background: var(--silver); color: #000; }

    .back {
      display: inline-block;
      margin-top: 20px;
      color: var(--gold);
      text-decoration: none;
      font-weight: 700;
      font-size: 14px;
    }
  </style>
</head>
<body>
  <main>
    <div class="icon">🔐</div>
    <h1>Subscribe to Add Children</h1>
    <p class="msg">
      To add your child and start their learning journey, choose a plan below.
      Each child will get 1 free trial quiz per subject plus full access to all paid quizzes.
    </p>

    <div class="plan-grid">
      <div class="plan-card silver-border">
        <span class="plan-badge badge-silver">Silver</span>
        <div class="plan-price">$5</div>
        <div class="plan-period">per month</div>
        <div class="plan-detail">Up to 2 children</div>
        <form method="POST" action="/payments/paynow/init">
          <input type="hidden" name="plan" value="silver">
          <button class="btn btn-silver" type="submit">💳 Get Silver</button>
        </form>
      </div>

      <div class="plan-card gold-border">
        <span class="plan-badge badge-gold">Gold</span>
        <div class="plan-price">$10</div>
        <div class="plan-period">per month</div>
        <div class="plan-detail">Up to 5 children</div>
        <form method="POST" action="/payments/paynow/init">
          <input type="hidden" name="plan" value="gold">
          <button class="btn" type="submit">💳 Get Gold</button>
        </form>
      </div>
    </div>

    <a href="/parent/dashboard" class="back">← Back to Dashboard</a>
  </main>
</body>
</html>
