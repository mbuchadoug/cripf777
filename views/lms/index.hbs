{{!-- views/lms/index.hbs --}}
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>CRIPFCnt Responsibility LMS</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />

  <style>
    :root{
      --gold:#D4AF37;
      --black:#000;
      --charcoal:#111;
      --white:#F5F5F5;
      --muted:#9AA3AD;
      --radius:14px;
      --shadow:0 20px 60px rgba(0,0,0,0.45);
      --maxw:1200px;
    }

    *{box-sizing:border-box}
    body{
      margin:0;
      font-family:Inter,system-ui,-apple-system,"Segoe UI",Roboto,Arial;
      background:linear-gradient(180deg,#050505,#0b0b0b);
      color:var(--white);
    }

    a{color:inherit;text-decoration:none}

    /* ---------- NAVBAR ---------- */
    .nav{
      display:flex;
      justify-content:space-between;
      align-items:center;
      padding:18px 26px;
      border-bottom:1px solid rgba(255,255,255,0.04);
    }

    .brand{
      font-weight:900;
      color:var(--gold);
      letter-spacing:0.6px;
    }

    .nav-links{
      display:flex;
      gap:14px;
      align-items:center;
      font-size:14px;
      font-weight:600;
      color:var(--muted);
    }

    .nav-links a:hover{color:var(--white)}

    /* ---------- LAYOUT ---------- */
    .container{
      max-width:var(--maxw);
      margin:40px auto;
      padding:0 24px;
    }

    /* ---------- HERO ---------- */
    .hero{
      position:relative;
      border-radius:18px;
      overflow:hidden;
      box-shadow:var(--shadow);
      background:#000;
    }

    .hero-bg{
      position:absolute;
      inset:0;
      background:url("/pics/pic3.jpeg") center/cover no-repeat;
      filter:brightness(0.4);
    }

    .hero-content{
      position:relative;
      padding:64px;
      max-width:760px;
    }

    .pill{
      display:inline-block;
      padding:6px 14px;
      border-radius:999px;
      background:rgba(212,175,55,0.14);
      color:var(--gold);
      font-size:13px;
      font-weight:800;
    }

    h1{
      margin:16px 0 12px;
      font-size:46px;
      font-weight:900;
      color:var(--gold);
      line-height:1.05;
    }

    .subtitle{
      font-size:18px;
      color:rgba(255,255,255,0.9);
    }

    .lead{
      margin-top:14px;
      font-size:15px;
      color:rgba(255,255,255,0.8);
      max-width:680px;
    }

    .cta-row{
      margin-top:26px;
      display:flex;
      gap:14px;
      flex-wrap:wrap;
    }

    .btn{
      padding:12px 18px;
      border-radius:10px;
      font-weight:800;
      font-size:14px;
      display:inline-block;
    }

    .btn-primary{
      background:var(--gold);
      color:#000;
    }

    .btn-outline{
      border:2px solid rgba(212,175,55,0.9);
      color:var(--white);
    }

    /* ---------- INFO GRID ---------- */
    .grid{
      margin-top:38px;
      display:grid;
      grid-template-columns:repeat(3,1fr);
      gap:18px;
    }

    .card{
      padding:22px;
      border-radius:14px;
      background:linear-gradient(180deg,rgba(255,255,255,0.02),rgba(0,0,0,0.12));
      border:1px solid rgba(255,255,255,0.04);
    }

    .card h3{
      margin:0 0 8px;
      color:var(--gold);
      font-size:18px;
    }

    .card p{
      margin:0;
      font-size:14px;
      color:var(--muted);
      line-height:1.5;
    }

    /* ---------- FOOTER CTA ---------- */
    .final{
      margin-top:42px;
      padding:26px;
      border-radius:14px;
      background:linear-gradient(90deg,#070707,#0e0e0e);
      display:flex;
      justify-content:space-between;
      align-items:center;
      border:1px solid rgba(255,255,255,0.04);
    }

    @media(max-width:900px){
      .hero-content{padding:36px}
      h1{font-size:34px}
      .grid{grid-template-columns:1fr}
      .final{flex-direction:column;gap:16px;text-align:center}
    }
  </style>
</head>

<body>

<!-- ================= NAV ================= -->
<nav class="nav">
  <div class="brand">CRIPFCnt LMS</div>

  <div class="nav-links">
    {{#if user}}
      <a href="/org/cripfcnt-school/dashboard">Dashboard</a>
      <a href="/auth/logout">Sign out</a>
    {{else}}
      <a href="/about">About</a>
      <a href="/lms/quiz">Preview Quiz</a>
      <a href="/auth/google">Sign in</a>
    {{/if}}
  </div>
</nav>

<!-- ================= MAIN ================= -->
<main class="container">

  <!-- HERO -->
  <section class="hero">
    <div class="hero-bg"></div>

    <div class="hero-content">
      <span class="pill">RESPONSIBILITY LMS</span>

      <h1>Responsibility Is Placement</h1>

      <div class="subtitle">
        The first Learning Management System built on structural responsibility,
        decision integrity, and the Social Contract Obligation Index (SCOI).
      </div>

      <div class="lead">
        CRIPFCnt trains responsibility as a civilizational function - not behaviour,
        not compliance, not morality. Learn how placement, duty and interpretation
        shape stability, leadership and collective trust.
      </div>

      <div class="cta-row">
        {{#if user}}
          <a class="btn btn-primary" href="/org/cripfcnt-school/dashboard">
            Go to Dashboard
          </a>
        {{else}}
          <a class="btn btn-primary" href="/auth/google">
            Sign in to Start Learning
          </a>
          <a class="btn btn-outline" href="/lms/quiz">
            Try a 5-Question Preview Quiz
          </a>
        {{/if}}
      </div>
    </div>
  </section>

  <!-- INFO GRID -->
  <section class="grid">
    <div class="card">
      <h3>Placement-Aligned Learning</h3>
      <p>
        Learn responsibility as structural placement - how decisions
        lock individuals into trajectories with consequences.
      </p>
    </div>

    <div class="card">
      <h3>Decision Simulations</h3>
      <p>
        Scenario-based quizzes train instinct, not memory.
        Every choice recalibrates interpretation and duty.
      </p>
    </div>

    <div class="card">
      <h3>Verified Certification</h3>
      <p>
        Complete simulations and exams to earn a CRIPFCnt
        Responsibility Certificate aligned to SCOI metrics.
      </p>
    </div>
  </section>

  <!-- FINAL CTA -->
  <section class="final">
    <div>
      <h3 style="margin:0;color:var(--gold)">Begin Your Responsibility Journey</h3>
      <p style="margin:6px 0 0;color:var(--muted)">
        Join the first civilization-aligned LMS designed for stability, clarity and purpose.
      </p>
    </div>

    {{#if user}}
      <a class="btn btn-primary" href="/org/cripfcnt-school/dashboard">
        Open Dashboard
      </a>
    {{else}}
      <a class="btn btn-primary" href="/auth/google">
        Sign in
      </a>
    {{/if}}
  </section>

</main>

</body>
</html>
