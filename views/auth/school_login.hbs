<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>School Login</title>
  <style>
    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Arial, sans-serif;
      background: #f3f5f9;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0;
    }

    .login-card {
      background: #ffffff;
      padding: 32px 28px;
      width: 100%;
      max-width: 380px;
      border-radius: 14px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.08);
    }

    h1 {
      margin: 0 0 20px;
      text-align: center;
      font-size: 22px;
      color: #222;
    }

    .error {
      background: #ffe6e6;
      color: #8b1d1d;
      padding: 10px 12px;
      border-radius: 8px;
      font-size: 14px;
      margin-bottom: 16px;
      text-align: center;
    }

    label {
      display: block;
      margin-bottom: 14px;
    }

    label span {
      display: block;
      font-size: 13px;
      color: #555;
      margin-bottom: 6px;
    }

    input {
      width: 100%;
      padding: 12px 14px;
      border-radius: 10px;
      border: 1px solid #dcdfe5;
      font-size: 15px;
      outline: none;
      transition: border-color 0.15s, box-shadow 0.15s;
    }

    input:focus {
      border-color: #4b2dbb;
      box-shadow: 0 0 0 3px rgba(75,45,187,0.12);
    }

    button {
      width: 100%;
      margin-top: 10px;
      padding: 12px;
      border-radius: 12px;
      border: none;
      background: #4b2dbb;
      color: #fff;
      font-size: 15px;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.15s, transform 0.05s;
    }

    button:hover {
      background: #3e25a0;
    }

    button:active {
      transform: scale(0.98);
    }

    .help-text {
      margin-top: 16px;
      font-size: 13px;
      color: #666;
      text-align: center;
    }
  </style>
</head>

<body>
  <div class="login-card">
    <h1>School Login</h1>

    {{#if error}}
      <div class="error">{{error}}</div>
    {{/if}}

    <form method="POST" action="/auth/school">
      <label>
        <span>School ID</span>
        <input name="loginId" placeholder="Student / Teacher ID" required />
      </label>

      <label>
        <span>Password</span>
        <input type="password" name="password" required />
      </label>

      <button type="submit">Log in</button>
    </form>

    <p class="help-text">
      Use the ID and password provided by your admin
    </p>
  </div>
</body>
</html>
