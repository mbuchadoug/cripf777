<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{title}}</title>
  <style>
    :root {
      --bg: #f5f7fa;
      --card-bg: #ffffff;
      --accent: #0066cc;
      --accent-soft: #e1edff;
      --accent-dark: #004c99;
      --border-soft: #e1e5ec;
      --text-main: #1f2933;
      --text-muted: #6b7280;
      --danger: #c0392b;
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      padding: 24px;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: var(--bg);
      color: var(--text-main);
    }

    .shell {
      max-width: 1100px;
      margin: 0 auto;
    }

    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 18px;
      gap: 12px;
    }

    h1 {
      margin: 0;
      font-size: 1.5rem;
      font-weight: 800;
      color: #003366;
    }

    .subtitle {
      margin-top: 4px;
      font-size: 0.95rem;
      color: var(--text-muted);
    }

    a.btn-link {
      text-decoration: none;
      font-size: 0.85rem;
      padding: 6px 10px;
      border-radius: 999px;
      border: 1px solid var(--border-soft);
      background: #fff;
      color: var(--accent-dark);
      display: inline-flex;
      align-items: center;
      gap: 6px;
    }

    a.btn-link:hover {
      border-color: var(--accent);
    }

    .layout-grid {
      display: grid;
      grid-template-columns: 1.2fr 2fr;
      gap: 20px;
      margin-bottom: 24px;
    }

    @media (max-width: 860px) {
      .layout-grid {
        grid-template-columns: 1fr;
      }
    }

    .card {
      background: var(--card-bg);
      border-radius: 14px;
      padding: 16px 18px;
      box-shadow: 0 8px 20px rgba(15, 23, 42, 0.04);
      border: 1px solid var(--border-soft);
    }

    .card-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 6px;
    }

    .card-title {
      font-size: 0.95rem;
      font-weight: 700;
      color: var(--accent-dark);
    }

    .metric-value {
      font-size: 1.9rem;
      font-weight: 800;
      color: #111827;
    }

    .metric-label {
      font-size: 0.85rem;
      color: var(--text-muted);
      margin-top: 4px;
    }

    .badge {
      display: inline-flex;
      align-items: center;
      gap: 4px;
      padding: 4px 9px;
      border-radius: 999px;
      background: var(--accent-soft);
      color: var(--accent-dark);
      font-size: 0.75rem;
      font-weight: 600;
    }

    .filters {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-top: 8px;
    }

    .filters label {
      font-size: 0.8rem;
      color: var(--text-muted);
      display: flex;
      flex-direction: column;
      gap: 4px;
    }

    .filters select,
    .filters input[type="number"],
    .filters input[type="text"] {
      padding: 6px 8px;
      border-radius: 8px;
      border: 1px solid var(--border-soft);
      font-size: 0.85rem;
      min-width: 90px;
    }

    .filters button {
      padding: 7px 13px;
      border-radius: 999px;
      border: none;
      background: var(--accent);
      color: #fff;
      font-size: 0.85rem;
      cursor: pointer;
      margin-left: 2px;
    }

    .filters button:hover {
      background: var(--accent-dark);
    }

    .pill {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 3px 10px;
      border-radius: 999px;
      background: #f3f4f6;
      font-size: 0.75rem;
      color: var(--text-muted);
      margin-top: 4px;
    }

    /* table */

    .table-card {
      overflow: hidden;
      padding: 0;
    }

    .table-wrapper {
      width: 100%;
      overflow-x: auto;
    }

    table {
      border-collapse: collapse;
      width: 100%;
      font-size: 0.85rem;
      min-width: 420px;
    }

    thead {
      background: #f9fafb;
    }

    th,
    td {
      padding: 10px 12px;
      border-bottom: 1px solid #e5e7eb;
      text-align: left;
      white-space: nowrap;
    }

    th {
      font-size: 0.75rem;
      letter-spacing: 0.03em;
      text-transform: uppercase;
      color: var(--text-muted);
    }

    tbody tr:nth-child(even) {
      background: #fafbff;
    }

    tbody tr:hover {
      background: #eef4ff;
    }

    .td-period {
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
      font-size: 0.8rem;
    }

    .td-count {
      font-weight: 600;
    }

    .td-count.strong {
      color: #059669;
    }

    .empty-state {
      padding: 24px;
      text-align: center;
      color: var(--text-muted);
      font-size: 0.9rem;
    }

    .empty-state span {
      display: inline-block;
      margin-top: 6px;
    }

    .hint {
      font-size: 0.75rem;
      color: var(--text-muted);
      margin-top: 6px;
    }

    .footer-note {
      margin-top: 14px;
      font-size: 0.75rem;
      color: var(--text-muted);
    }
  </style>
</head>
<body>
  <div class="shell">
    <header>
      <div>
        <h1>Unique visitors</h1>
        <p class="subtitle">
          High-level view of how many distinct visitors have interacted with your CRIPFCnt surfaces.
        </p>
      </div>
      <div>
        <a href="/admin/users" class="btn-link">
          ← Back to users
        </a>
      </div>
    </header>

    <div class="layout-grid">
      <!-- Left: KPI + filters -->
      <section class="card">
        <div class="card-header">
          <div>
            <div class="card-title">Global unique footprint</div>
            <div class="metric-value">{{totalUnique}}</div>
            <div class="metric-label">
              Total distinct visitor IDs stored in <code>UniqueVisit</code>
            </div>
          </div>
          <span class="badge">
            ● Live metric
          </span>
        </div>

        <form class="filters" method="get" action="/admin/unique-visitors">
          <label>
            Period
            <select name="period">
              <option value="day" {{#if isDay}}selected{{/if}}>By day</option>
              <option value="month" {{#if isMonth}}selected{{/if}}>By month</option>
              <option value="year" {{#if isYear}}selected{{/if}}>By year</option>
            </select>
          </label>

          <label>
            Lookback (days)
            <input type="number" name="days" min="1" max="365" value="{{days}}">
          </label>

          <label>
            Path filter
            <input
              type="text"
              name="path"
              placeholder="/ or /audit"
              value="{{path}}"
            >
          </label>

          <button type="submit">Apply</button>
        </form>

        {{#if path}}
          <div class="pill">
            Filtering by path:
            <strong>{{path}}</strong>
          </div>
        {{/if}}

        <p class="hint">
          Tip: switch to <strong>month</strong> or <strong>year</strong> view
          to see longer-term placement-level demand trends.
        </p>
      </section>

      <!-- Right: table -->
      <section class="card table-card">
        <div class="card-header" style="padding: 16px 18px 0 18px;">
          <div class="card-title">Time-series</div>
          <span class="badge">
            {{period}} view
          </span>
        </div>

        <div class="table-wrapper">
          {{#if series.length}}
            <table>
              <thead>
                <tr>
                  <th>Bucket ({{period}})</th>
                  <th>Unique visitors</th>
                </tr>
              </thead>
              <tbody>
                {{#each series}}
                  <tr>
                    <td class="td-period">{{_id}}</td>
                    <td class="td-count {{#if @last}}strong{{/if}}">
                      {{uniqueCount}}
                    </td>
                  </tr>
                {{/each}}
              </tbody>
            </table>
          {{else}}
            <div class="empty-state">
              No unique visitor data found for this filter.
              <span>Try widening the period or removing the path filter.</span>
            </div>
          {{/if}}
        </div>
      </section>
    </div>

    <p class="footer-note">
      Unique visitor IDs are calculated per <strong>day × visitor × path</strong> and aggregated into
      <code>day</code>, <code>month</code>, and <code>year</code> buckets for placement-level analysis.
    </p>
  </div>
</body>
</html>
