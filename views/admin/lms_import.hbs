{{!-- views/admin/lms_import.hbs --}}
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Import LMS Questions</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <style>
    body{font-family:Inter,system-ui,Arial;margin:18px;background:#fff;color:#111}
    .wrap{max-width:980px;margin:0 auto}
    textarea{width:100%;height:360px;font-family:monospace;padding:12px;border:1px solid #ccc;border-radius:6px}
    .note{color:#666;margin-bottom:10px}
    .btn{display:inline-block;padding:10px 14px;border-radius:6px;background:#0b6fb7;color:#fff;border:none;cursor:pointer}
    .secondary{background:#eee;color:#222;border:1px solid #ccc}
    .links{margin-top:12px}
    pre.block{background:#f7f7f7;border:1px solid #e6e6e6;padding:12px;border-radius:6px;white-space:pre-wrap}
  </style>
</head>
<body>
  <div class="wrap">
    <h1>Import LMS Questions (paste)</h1>

    <p class="note">
      Upload a plain text file (.txt) containing multiple-choice questions in the format used in your spec (number, a) b) c) d), and a "Correct Answer:" line).
      Alternatively paste the content into the textarea and click <strong>Import</strong>.
      The importer saves a fallback file to <code>/mnt/data/responsibilityQuiz.txt</code> so the quiz API can pick it up.
    </p>

    <form method="post" enctype="multipart/form-data" action="/admin/lms/import">
      <div style="margin-bottom:12px">
        <label><strong>Upload .txt file</strong></label><br/>
        <input type="file" name="file" accept=".txt" />
      </div>

      <div style="text-align:center;margin:8px 0;color:#888">— or —</div>

      <div>
        <label><strong>Paste questions (text)</strong></label>
        <textarea name="text" placeholder="Paste the quiz text here...">{{{text}}}</textarea>
      </div>

      <div style="margin-top:10px">
        <label style="font-weight:700"><input type="checkbox" name="save" value="1" /> Also save parsed questions to DB (Question model)</label>
      </div>

      <div style="margin-top:14px">
        <button class="btn" type="submit">Import</button>
        <button class="btn secondary" type="reset" style="margin-left:8px">Reset</button>
      </div>
    </form>

    <div class="links">
      <p>
        After importing you can visit <a href="/admin/lms/questions">Manage Questions</a> (admin) or open the quiz UI at <a href="/lms/quiz">/lms/quiz</a>.
      </p>
      <p style="color:#777;font-size:13px">Note: if "save to DB" is checked the server will attempt to insert parsed questions into the Questions collection (requires a Question model at <code>models/question.js</code>).</p>
    </div>
  </div>
</body>
</html>
