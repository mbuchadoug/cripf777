{{!-- views/admin/special_scoi_import.hbs --}}
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Import Special SCOI Audit</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <style>
    body {
      font-family: system-ui, -apple-system, "Segoe UI", Arial;
      background: #0b0b0b;
      color: #f5f5f5;
      margin: 0;
      padding: 24px;
    }

    h1 {
      color: #D4AF37;
      margin-bottom: 8px;
    }

    p {
      color: #bdbdbd;
      max-width: 720px;
    }

    .box {
      background: #121212;
      border: 1px solid rgba(212,175,55,0.25);
      border-radius: 14px;
      padding: 24px;
      max-width: 720px;
      margin-top: 20px;
    }

    label {
      display: block;
      margin-bottom: 8px;
      font-weight: 600;
    }

    input[type="file"] {
      margin-bottom: 16px;
      color: #ccc;
    }

    button {
      background: #D4AF37;
      color: #000;
      border: none;
      border-radius: 8px;
      padding: 10px 18px;
      font-weight: 800;
      cursor: pointer;
    }

    button:hover {
      opacity: 0.9;
    }

    .success {
      background: #0f2e1a;
      border: 1px solid #2ecc71;
      color: #c8f7dc;
      padding: 14px;
      border-radius: 10px;
      margin-bottom: 16px;
    }

    .error {
      background: #2a0f0f;
      border: 1px solid #c0392b;
      color: #f6c1c1;
      padding: 14px;
      border-radius: 10px;
      margin-bottom: 16px;
    }

    .note {
      margin-top: 16px;
      font-size: 14px;
      color: #999;
    }
  </style>
</head>

<body>

  {{> navbar}}

  <h1>Import Special SCOI Audit</h1>

  <p>
    This tool permanently imports a <strong>Special SCOI Audit Report</strong>
    (non-placement, structured audit) into the CRIPFCnt system.
    Imported audits are immediately available for admin review and marketplace sale.
  </p>

  <div class="box">

    {{#if success}}
      <div class="success">
        {{success}}
      </div>
    {{/if}}

    {{#if error}}
      <div class="error">
        {{error}}
      </div>
    {{/if}}

    <form
      method="POST"
      action="/admin/special-scoi-import"
      enctype="multipart/form-data"
    >
      <label for="auditFile">Special SCOI JSON File</label>
      <input
        type="file"
        name="auditFile"
        id="auditFile"
        accept=".json"
        required
      />

      <button type="submit">
        Import Special SCOI Audit
      </button>
    </form>

    <div class="note">
      • File must be valid JSON<br/>
      • Audit will be marked as <strong>Special SCOI</strong><br/>
      • Default marketplace price: <strong>$299</strong><br/>
      • This action is permanent
    </div>

  </div>

</body>
</html>
