{{!-- views/wifi/success.hbs --}}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>WiFi Access Granted</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    .container {
      background: white;
      border-radius: 20px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.3);
      padding: 50px 40px;
      max-width: 600px;
      width: 100%;
    }

    .success-icon {
      text-align: center;
      font-size: 64px;
      margin-bottom: 20px;
      animation: bounce 0.6s;
    }

    @keyframes bounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-20px); }
    }

    h1 {
      text-align: center;
      font-size: 28px;
      color: #333;
      margin-bottom: 10px;
    }

    .subtitle {
      text-align: center;
      color: #666;
      margin-bottom: 40px;
      font-size: 16px;
    }

    .credentials-box {
      background: #f8f9fa;
      border: 2px solid #e0e0e0;
      border-radius: 15px;
      padding: 30px;
      margin-bottom: 30px;
    }

    .credential-row {
      margin-bottom: 25px;
    }

    .credential-row:last-child {
      margin-bottom: 0;
    }

    .credential-label {
      font-size: 12px;
      text-transform: uppercase;
      color: #999;
      font-weight: 600;
      letter-spacing: 1px;
      margin-bottom: 8px;
    }

    .credential-value {
      font-size: 24px;
      font-weight: 700;
      color: #333;
      background: white;
      padding: 15px 20px;
      border-radius: 10px;
      border: 1px solid #ddd;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .copy-btn {
      background: #667eea;
      color: white;
      border: none;
      padding: 8px 16px;
      border-radius: 8px;
      cursor: pointer;
      font-size: 14px;
      font-weight: 600;
      transition: all 0.2s;
    }

    .copy-btn:hover {
      background: #5568d3;
      transform: scale(1.05);
    }

    .copy-btn:active {
      transform: scale(0.95);
    }

    .copy-btn.copied {
      background: #38ef7d;
    }

    .info-box {
      background: #fff3cd;
      border: 1px solid #ffc107;
      border-radius: 10px;
      padding: 20px;
      margin-bottom: 20px;
    }

    .info-box strong {
      color: #856404;
      display: block;
      margin-bottom: 10px;
    }

    .info-box ul {
      margin-left: 20px;
      color: #856404;
    }

    .info-box li {
      margin-bottom: 5px;
    }

    .expiry-info {
      text-align: center;
      padding: 20px;
      background: #f8f9fa;
      border-radius: 10px;
      margin-bottom: 20px;
    }

    .expiry-info .time {
      font-size: 32px;
      font-weight: 800;
      color: #11998e;
      margin-bottom: 5px;
    }

    .expiry-info .label {
      color: #666;
      font-size: 14px;
    }

    .back-btn {
      display: block;
      width: 100%;
      padding: 15px;
      background: #667eea;
      color: white;
      text-align: center;
      text-decoration: none;
      border-radius: 10px;
      font-weight: 600;
      transition: all 0.2s;
    }

    .back-btn:hover {
      background: #5568d3;
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(102, 126, 234, 0.3);
    }

    @media (max-width: 600px) {
      .container {
        padding: 40px 30px;
      }

      .credential-value {
        font-size: 18px;
        flex-direction: column;
        gap: 10px;
      }

      .expiry-info .time {
        font-size: 24px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="success-icon">✅</div>
    <h1>Access Granted!</h1>
    <p class="subtitle">Here are your WiFi credentials</p>

    <div class="credentials-box">
      <div class="credential-row">
        <div class="credential-label">📶 Network Name (SSID)</div>
        <div class="credential-value">
          <span id="ssid">{{wifiSSID}}</span>
          <button class="copy-btn" onclick="copyText('ssid', this)">Copy</button>
        </div>
      </div>

      <div class="credential-row">
        <div class="credential-label">🔑 Password</div>
        <div class="credential-value">
          <span id="password">{{wifiPassword}}</span>
          <button class="copy-btn" onclick="copyText('password', this)">Copy</button>
        </div>
      </div>
    </div>

    <div class="expiry-info">
      <div class="time">{{hoursRemaining}}h {{minutesRemaining}}m</div>
      <div class="label">Access expires in</div>
    </div>

    <div class="info-box">
      <strong>⚠️ Important Information:</strong>
      <ul>
        <li>Your access code: <strong>{{code}}</strong></li>
        <li>Expires: {{expiresAt}}</li>
        {{#if usesRemaining}}
          <li>Uses remaining: {{usesRemaining}}</li>
        {{/if}}
        <li>Do not share this code with others</li>
        <li>Save these credentials for reconnecting</li>
      </ul>
    </div>

    <a href="/wifi" class="back-btn">← Back to Portal</a>
  </div>

  <script>
    function copyText(elementId, button) {
      const text = document.getElementById(elementId).textContent;
      
      // Copy to clipboard
      navigator.clipboard.writeText(text).then(() => {
        // Show success feedback
        const originalText = button.textContent;
        button.textContent = '✓ Copied!';
        button.classList.add('copied');
        
        setTimeout(() => {
          button.textContent = originalText;
          button.classList.remove('copied');
        }, 2000);
      }).catch(err => {
        alert('Failed to copy. Please copy manually.');
      });
    }

    // Auto-copy password on load (optional)
    window.addEventListener('load', () => {
      // Uncomment to auto-copy password
      // copyText('password', document.querySelector('.copy-btn'));
    });
  </script>
</body>
</html>
